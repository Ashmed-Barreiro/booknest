# BookNest Backend

## Setup
1) Copy .env.example to .env
2) Install deps:
   npm i
3) Create DB + migrate:
   npx prisma migrate dev --name init
4) Run:
   npm run dev

API runs at http://localhost:4000
